# Login / Logout messages

idp.login.loginTo = Accedi a

idp.login.username = Username
idp.login.password = Password

idp.login.donotcache = Non ricordare la login

idp.login.login = Login

idp.login.forgotPassword = Dimenticata la password?
idp.login.needHelp = Serve aiuto?

# Expiring password example messages

idp.login.expiringSoon = La tua password sta per scadere!
idp.login.changePassword = Per creare una nuova password ora, vai a 
idp.login.proceedBegin = La tua login continuer&agrave; entro 20 secondi, se non vieni reindirizzato automaticamente clicca
idp.login.proceedHere = qui
idp.login.proceedEnd = per continuare.

idp.login.goback = Torna alla pagina di login
idp.login.optionalattrs = Attributi opzionali
idp.login.nooptionalattrs = Nessun attributo opzionale
idp.login.requestedattrs = Attributi Richiesti
idp.login.norequestedattrs = Nessun attributo richiesto
idp.login.unknown = Sconosciuto
idp.login.pwdexpired = Password scaduta
idp.login.invalidpwd = Password non valida
idp.login.changepwd = Cambiala
idp.login.locked = Account temporaneamente bloccato
idp.login.wrong = Nome utente o password errati. Prova ancora
idp.login.loginto = Accedi a
idp.login.spdescr = Descrizione del SP
idp.login.moreinfo = Informazioni aggiuntive sul SP

# Classified Login Error messages

UnknownUsername = bad-username
InvalidPassword = bad-password
ExpiredPassword = expired-password
AccountLocked = account-locked

bad-username.message = Lo username inserito non &egrave; stato identificato.

bad-password.message = La password inserita non &egrave; corretta.

expired-password.message = La tua password &egrave; scaduta.

account-locked.message = Il tuo account &egrave; bloccato.
