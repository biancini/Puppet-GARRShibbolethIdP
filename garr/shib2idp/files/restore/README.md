Files that must be put here to perform a restore of LDAP DB and MySQL DB
========================================================================

1. ${idp-hostname}-mysql.tar.gz
2. ${idp-hostname}-ldap.tar.gz

